import React from 'react';
import {Input, Divider} from 'antd';

function Header() {
    return (
        <div>
            <Input/>
            <Divider />
        </div>
    );
}

export default Header;
